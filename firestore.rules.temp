rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Allow authenticated users to read/write everything
    // TODO: Replace with proper rules from firestore.rules
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
